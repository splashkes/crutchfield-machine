# Build artifacts
*.o
feedback.exe
feedback
*.dSYM/
feedback-macos-arm64/
feedback-macos-arm64.zip
macOS/feedback.app/

# Recorded output (EXR sequences + encoded MP4s are large)
recordings/
screenshots/

# Controller capture logs
macOS/*.log

# Distribution bundle from `make dist`
feedback-windows-x64/
feedback-windows-x64.zip
feedback-linux-x64/
feedback-linux-x64.tar.gz
feedback-macos-arm64/
feedback-macos-arm64.zip

# Out-of-tree build dirs for platform ports (linux/, macOS/)
linux/build/
macOS/build/
macOS/feedback.app/

# User-local input rebindings (regenerated on first launch)
bindings.ini

# OS junk
Thumbs.db
.DS_Store

# Claude session state
.claude/
vendor/quickjs/*.o
